# Copy this file to `.env` locally (do NOT commit secrets).
# Update values and keep `.env` out of source control.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FLASK_SECRET_KEY=
REDIRECT_URI=

# Example: REDIRECT_URI=https://<your-render-service>.onrender.com/callback